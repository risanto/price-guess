export type UserData = {
  email: string;
  phone: string;
  points: number;
};

export type UserProfile = {
  id?: number;
  created_at?: number;
  email: string;
  role: string;
  points: number;
};
